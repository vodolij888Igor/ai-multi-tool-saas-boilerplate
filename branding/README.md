# Branding

- **app-name**: Display name of the product.
- **theme.json**: Primary, secondary, accent colors and sidebar backgrounds for light/dark.
- **logo**: Place logo assets here (e.g. `logo.png`, `favicon.ico`) for quick rebranding.

To rebrand: update `app-name.txt`, `theme.json`, and replace logo files; then run the app.
